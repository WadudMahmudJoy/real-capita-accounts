"use client";

import { FormEvent, useEffect, useState } from "react";
import Link from "next/link";
import { useParams, useRouter } from "next/navigation";
import { ArrowLeft } from "lucide-react";
import { ApiError, getBookableItem, getProjects, toErrorMessage, updateBookableItem, type BookableItem, type BookableItemCategory, type BookableItemInput, type BookableItemStatus, type Project } from "@/lib/api";
import { Button, Card, CardHeader, Field, LoadingPanel, Notice, PageIntro, Select, TextArea, TextInput } from "../../../_components/ui";
import { BOOKABLE_ITEM_CATEGORY_OPTIONS, BOOKABLE_ITEM_EDITABLE_STATUS_OPTIONS, FieldErrorText, nullableOptionalString, parseMoneyInput, projectLabel } from "../../../customer-booking";

type FormState = { itemCode: string; projectId: string; category: BookableItemCategory; itemIdentifier: string; block: string; zone: string; phase: string; sizeOrArea: string; shareQuantity: string; basePrice: string; status: BookableItemStatus; notes: string };

function toForm(item: BookableItem): FormState {
  return { basePrice: String(item.basePrice), block: item.block ?? "", category: item.category, itemCode: item.itemCode, itemIdentifier: item.itemIdentifier, notes: item.notes ?? "", phase: item.phase ?? "", projectId: item.projectId, shareQuantity: item.shareQuantity ? String(item.shareQuantity) : "", sizeOrArea: item.sizeOrArea ?? "", status: item.status, zone: item.zone ?? "" };
}

export default function EditBookableItemPage() {
  const params = useParams<{ id: string }>();
  const router = useRouter();
  const [status, setStatus] = useState<"loading" | "ready" | "error">("loading");
  const [loadError, setLoadError] = useState<string | null>(null);
  const [projects, setProjects] = useState<Project[]>([]);
  const [form, setForm] = useState<FormState | null>(null);
  const [fieldErrors, setFieldErrors] = useState<Record<string, string>>({});
  const [formError, setFormError] = useState<string | null>(null);
  const [isSaving, setIsSaving] = useState(false);

  useEffect(() => { const controller = new AbortController(); async function load() { try { const [item, projectData] = await Promise.all([getBookableItem(params.id, controller.signal), getProjects(controller.signal)]); setForm(toForm(item)); setProjects(projectData.filter((project) => project.isActive || project.id === item.projectId)); setStatus("ready"); } catch (caught) { if (controller.signal.aborted) return; if (caught instanceof ApiError && caught.isUnauthorized) { router.replace("/login"); return; } setLoadError(toErrorMessage(caught)); setStatus("error"); } } void load(); return () => controller.abort(); }, [params.id, router]);

  function validate(current: FormState): boolean { const next: Record<string, string> = {}; const basePrice = parseMoneyInput(current.basePrice); const shareQuantity = parseMoneyInput(current.shareQuantity); if (!current.projectId) next.projectId = "Project is required."; if (!current.itemIdentifier.trim()) next.itemIdentifier = "Item identifier is required."; if (basePrice === undefined || Number.isNaN(basePrice) || basePrice <= 0) next.basePrice = "Base price must be a positive amount."; if (current.shareQuantity.trim() && (shareQuantity === undefined || Number.isNaN(shareQuantity) || shareQuantity < 0)) next.shareQuantity = "Share quantity must be zero or positive."; setFieldErrors(next); return Object.keys(next).length === 0; }

  async function handleSubmit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); if (!form) return; setFormError(null); if (!validate(form)) return; const basePrice = parseMoneyInput(form.basePrice) ?? 0; const shareQuantity = parseMoneyInput(form.shareQuantity); const payload: Partial<BookableItemInput> = { basePrice, block: nullableOptionalString(form.block), category: form.category, itemIdentifier: form.itemIdentifier.trim(), notes: nullableOptionalString(form.notes), phase: nullableOptionalString(form.phase), projectId: form.projectId, ...(shareQuantity === undefined ? {} : { shareQuantity }), sizeOrArea: nullableOptionalString(form.sizeOrArea), status: form.status, zone: nullableOptionalString(form.zone) }; setIsSaving(true); try { const updated = await updateBookableItem(params.id, payload); router.replace(`/app/bookable-items/${updated.id}`); router.refresh(); } catch (caught) { if (caught instanceof ApiError && caught.isUnauthorized) { router.replace("/login"); return; } setFormError(toErrorMessage(caught)); } finally { setIsSaving(false); } }

  return <div className="flex flex-col gap-6"><div className="flex items-center justify-between gap-3"><PageIntro title="Edit bookable item" description="Conservative item edits remain backend-constrained and never shortcut booking creation." /><Link href={`/app/bookable-items/${params.id}`}><Button variant="ghost"><ArrowLeft aria-hidden="true" className="size-4" />Back to item</Button></Link></div>{status === "loading" ? <LoadingPanel message="Loading bookable item..." /> : null}{status === "error" ? <Notice tone="error">{loadError}</Notice> : null}{status === "ready" && form ? <Card><CardHeader title="Bookable item form" description="Item code is generated and read-only. Status edits are conservative and backend constrained." /><form className="mt-6 flex flex-col gap-5" onSubmit={handleSubmit}><div className="grid gap-5 sm:grid-cols-2"><Field htmlFor="item-code" label="Item code"><TextInput id="item-code" value={form.itemCode} readOnly disabled /></Field><Field htmlFor="item-project" label="Project" required><Select id="item-project" value={form.projectId} onChange={(event) => setForm({ ...form, projectId: event.target.value })}><option value="">Select project</option>{projects.map((project) => <option key={project.id} value={project.id}>{projectLabel(project)}</option>)}</Select><FieldErrorText message={fieldErrors.projectId ?? null} /></Field><Field htmlFor="item-category" label="Category" required><Select id="item-category" value={form.category} onChange={(event) => setForm({ ...form, category: event.target.value as BookableItemCategory })}>{BOOKABLE_ITEM_CATEGORY_OPTIONS.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</Select></Field><Field htmlFor="item-status" label="Current status"><Select id="item-status" value={form.status} onChange={(event) => setForm({ ...form, status: event.target.value as BookableItemStatus })}>{form.status === "BOOKED" ? <option value="BOOKED">Booked</option> : null}{BOOKABLE_ITEM_EDITABLE_STATUS_OPTIONS.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</Select></Field><Field htmlFor="item-identifier" label="Item identifier" required><TextInput id="item-identifier" value={form.itemIdentifier} onChange={(event) => setForm({ ...form, itemIdentifier: event.target.value })} required /><FieldErrorText message={fieldErrors.itemIdentifier ?? null} /></Field><Field htmlFor="item-base-price" label="Base price" required><TextInput id="item-base-price" inputMode="decimal" value={form.basePrice} onChange={(event) => setForm({ ...form, basePrice: event.target.value })} required /><FieldErrorText message={fieldErrors.basePrice ?? null} /></Field><Field htmlFor="item-block" label="Block"><TextInput id="item-block" value={form.block} onChange={(event) => setForm({ ...form, block: event.target.value })} /></Field><Field htmlFor="item-zone" label="Zone"><TextInput id="item-zone" value={form.zone} onChange={(event) => setForm({ ...form, zone: event.target.value })} /></Field><Field htmlFor="item-phase" label="Phase"><TextInput id="item-phase" value={form.phase} onChange={(event) => setForm({ ...form, phase: event.target.value })} /></Field><Field htmlFor="item-size" label="Size / Area"><TextInput id="item-size" value={form.sizeOrArea} onChange={(event) => setForm({ ...form, sizeOrArea: event.target.value })} /></Field><Field htmlFor="item-share-quantity" label="Share quantity"><TextInput id="item-share-quantity" inputMode="decimal" value={form.shareQuantity} onChange={(event) => setForm({ ...form, shareQuantity: event.target.value })} /><FieldErrorText message={fieldErrors.shareQuantity ?? null} /></Field><Field className="sm:col-span-2" htmlFor="item-notes" label="Notes"><TextArea id="item-notes" value={form.notes} onChange={(event) => setForm({ ...form, notes: event.target.value })} /></Field></div><Notice tone="info">SOLD, CANCELLED, and HOLD are administrative item statuses only. They do not imply final accounting or legal completion.</Notice>{formError ? <Notice tone="error">{formError}</Notice> : null}<div className="flex flex-wrap gap-3"><Button disabled={isSaving} type="submit">{isSaving ? "Saving..." : "Save item"}</Button><Link href={`/app/bookable-items/${params.id}`}><Button variant="secondary">Cancel</Button></Link></div></form></Card> : null}</div>;
}
